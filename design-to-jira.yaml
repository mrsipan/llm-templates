prompt: |

  This is a document that propose a certain system design that must be
  implemented using tools such as Terraform, Python, Puppet, Shell scripts,
  etc. Or only such tools specified in the document if any have been
  specifically mentioned.

  Create a set of Jira tickets that would split the work of implementing such a
  system:

  $input

system_fragments:

  - formal

